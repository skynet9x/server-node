`use strict`;

const JsonStorage = require(`${__dirname}/../../index.js`);

// init storage
const store = new JsonStorage(`${__dirname}/storage`);

const obj = store.getAllSync();
console.log(obj);

// get a whole dir
const dir1 = store.getAllSync(`cars`);
console.log(dir1);
