`use strict`;

const checkDirname = require(`${__dirname}/../helper/checkDirname.js`);

function getDirAllSync(_this, dir) {

  checkDirname(dir);

  const all = {};

  _this.forEachSync(dir, (json, file) => {
    all[file] = json;
  });

  return all;
}

module.exports = getDirAllSync;
