`use strict`;

const checkDirname = require(`${__dirname}/../helper/checkDirname.js`);

async function getDirAll(_this, dir) {
  checkDirname(dir);

  const all = {};

  await _this.forEach(dir, (json, file) => {
    all[file] = json;
  });

  return all;
}

module.exports = getDirAll;
